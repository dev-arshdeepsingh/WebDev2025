// && -> and
// || -> or
// ! -> reverse or not

let isLoggedin = true;
let ispaid = true;

// console.log(isLoggedin && ispaid);

let isEmailuser = true;
let isGoogleuser = false;

console.log(isEmailuser || isGoogleuser);

//We can add as many conditions as we want and based on whether they are true or false, the result will be true or false
