//import { useState } from 'react' //pata nhi kya hai, hatao isko!

//import './App.css' because no more it exists

import Chai from './chai'

function App() {
  

  return (

    <Chai/>
    //<h1> Yokosu Watashino Soul Society Yaare Yaare | Sosuke Aizen</h1>  //We can wrap this both in a div. In react, <div></div> or <></> is same. So we use "<> <Chai/> <h1>Hello World</h1> </>" to wrap multiple elements.
  )
} //can return only one element. So if you want to return multiple elements, you have to wrap them in a single parent element.

export default App  //app function exported using es6 module syntax

//We also call <></> as fragments.
